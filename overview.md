# Overview

High-level summary of the vault.

This vault currently tracks notes on the [[Strait of Hormuz]] and [[Sentry error monitoring]] based on raw captures.

Recent ingest includes a German Sentry landing page with a trademark notice, a Sentry docs snippet about distributed tracing, and Sentry AI docs snippets about assistants, the MCP server, agent skills, hooks, and dotagents, including declarations in `agents.toml`, install-time skill copying, `agents.lock`, and the default `gitignore = false` behavior for committed skills.
